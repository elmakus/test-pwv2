# Task Card

- Card ID: M03-T01
- Included scope: Apply the smallest compatible repair in app/settings_store.py so save_setting persists the complete updated settings map to the existing staged file and atomically promotes that staged file to the durable settings path before updating in-memory state or returning success.
- Excluded scope: Storage-backend replacement, schema/default migration, startup behavior changes outside the existing durable-path read, unrelated settings behavior changes, and final broad verification beyond the bounded persistence regression suite.
- Authority refs: requirements/ISSUE_USER_SETTING_PERSISTENCE.md, decisions/NONDESTRUCTIVE_USER_SETTING_PERSISTENCE.md, planning/USER_SETTING_PERSISTENCE_PLAN.md
- Dependencies: implementation/workstreams/user-setting-persistence-restart/results/M02-T01.md@fd864837b6b514731823d957f00d55e414b334db:22040b172dbcb5d8f89bb08977f03bad1fd8e507
- Acceptance: save_setting preserves the complete existing settings map, writes the staged full state first, promotes it atomically with Path.replace to the durable path, updates in-memory state and returns success only after promotion succeeds, preserves unrelated/unknown keys, and leaves the prior durable state readable if either staging write or promotion fails.
- Required tests/readback: Run the full settings persistence unittest suite and require all three M02 tests GREEN; read back app/settings_store.py and confirm no storage replacement, destructive migration/reset, or startup default-path expansion was introduced.
- Review requirement: required
- Technical contract: none
